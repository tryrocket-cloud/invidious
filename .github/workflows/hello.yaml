name: Hello

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  hello:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
    
      - name: Run a script
        run: echo "This action runs every 2 minutes"
