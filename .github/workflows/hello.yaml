name: Hello

on:
  push:
    branches:
      - main
  schedule:
    - cron: '* * * * *'

jobs:
  hello:
    runs-on: self-hosted

    steps:
      - name: Run a script
        run: echo "This action runs every 2 minutes"
